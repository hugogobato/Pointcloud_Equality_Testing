# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Environments (uv-managed locally; Colab installs from pyproject.toml)
.venv/
env/
venv/

# Caches
.cache/
.pytest_cache/
.ipynb_checkpoints/
.ruff_cache/
.coverage
htmlcov/

# Run artefacts: reproductions write here, results are committed deliberately
# (see notebooks/README.md), not swept up automatically.
results/*.json
results/*.csv
!results/.gitkeep
# ...except the Phase 0.5 acceptance runs, which are evidence for docs/phase0_completion.md
!results/*_reps500.json
# ...and Figure 1's data, which is the Phase 2 gate's evidence (theory/WP2_covariate_shift.md section 6.1)
!results/phase2_figure1.json

# Windows alternate-data-stream markers, created when Colab output is
# downloaded through a browser onto the WSL mount.
*:Zone.Identifier


Literature_Review/
RESEARCH_DECISION.md
RESEARCH_PLAN_P1_TwoSample.md
RESEARCH_PLAN_P2_FiltrationEquivalence.md